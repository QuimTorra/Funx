Len l 
{
    if l = [] { 0 }
    1 + (Len l[1:])
}

Max l 
{
    if (Len l) = 0 { 0 }
    if (Len l) = 1 { l[0] }
    if (l[0] > (Max l[1:])) {l[0] }
    else { Max l[1:] }
}

Reverse l
{
    res <- []
    for x in l 
    {
        res <- x + res
    }
    res
}